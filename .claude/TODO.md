# TODO — linguaflow.app Website

## In Arbeit
- [ ] **Interaktive Methoden-Erklärseite (Session „linguaflow-interactive-explanation", Stand 2026-08-28)** — Christians Idee: eigene Website-Seite, die die Birkenbihl-inspirierte Methode interaktiv erklärt (Sprachauswahl → Überblick → antippbarer Beispieltext in 3/4-Zeilen-Ansicht wie in der App → vorgelesener Erklärtext mit Wort-Hervorhebung → Download-Buttons). Stand: Grill-/Planungs-Session läuft, Runde 1 der Entwurfsfragen (Q1–Q9) an Christian gestellt, Antworten stehen aus. Recherche abgeschlossen: (a) Website ist statisch/GitHub Pages, interaktive Inhalte müssen in eigene JSON-Dateien (DeepL-Pipeline übersetzt kein JS); (b) App bündelt fertige Lernweg-Inhalte pro Sprachpaar (`LearningPath/LearningPathContent_*.json` im iOS-Repo, Schema: Wortpaare [fremd, Bedeutung, Umschrift?] + sinngemäße Übersetzung) → wiederverwendbar; (c) im App-Backend existiert bereits ein Skript-Muster für vorab generiertes Audio mit Wort-Zeitstempeln (`functions/scripts/generateLearningPathAudio.mjs`, ElevenLabs with-timestamps) → Vorbild für Website-Audio ohne API-Key im Browser; (d) `npx create-ai-eng-app` geprüft und verworfen (Next.js/Supabase-Kurs-Kit, passt nicht zu statischer Site). Glossar der Fachbegriffe neu in `CONTEXT.md` (Repo-Root). **Runde 1 beantwortet (2026-08-28):** volle Sprachpaar-Matrix mit den auf der Website gelisteten App-Sprachen; Beispieltext wird neu erfunden (2–4 Sätze, Thema „zurück in deinen Beruf dank Sprachkenntnissen"); Audio vorab generieren (kein Key im Browser); eigene Unterseite + App-Modus; scrollbare Seite statt Wizard; Wort-Antippen = ab diesem Wort abspielen (keine Erklärung, kein langes Drücken); keine Level-Abfrage (beide Wege zeigen); eigenständig ohne Free-Tool-Andockung. **Runde 2 beantwortet (2026-08-28):** Beispieltext freigegeben („Ich habe viele Jahre in meinem Beruf gearbeitet. …"); Erklärtext kompakt (~2.500 Zeichen) und komplett kapitelweise vorgelesen; Dekodierungen über die App-Pipeline (decodeText, ~10–20 €); DeepL-Übersetzungen ok (~2–3 €); `methode.html` in allen 33 Sprachen (~2–4 €); index-Verlinkung per Cache-Trick; App-Stimmen aus voiceCatalog; alle Daten im Website-Repo. ElevenLabs-Befund: Pro-Tarif 500k Zeichen/Monat, aktuell ~709k verbraucht (über Limit), Reset ~9. September — Audio-Zeitpunkt ist die letzte offene Frage. **Planungsergebnis: `.claude/plan-methode-seite.md`** (Schritte mit Check-Kriterien), Grundsatzentscheidungen in DECISIONS.md (2026-08-28). **Bestätigt (2026-08-28):** Audio sofort generieren (Aufpreis akzeptiert); Umsetzung läuft auf Christians Wunsch **in dieser Session** (Branch `claude/linguaflow-interactive-explanation-8dd7f2`) nach `.claude/plan-methode-seite.md`. **Nächster Schritt:** Plan-Schritte 1–10; harter Haltepunkt: Freigabe des deutschen Erklärtexts vor Übersetzung/Vertonung. **Nicht ohne Rückfrage:** Pipeline-Registrierung vor finalem deutschen Seitentext; unbezifferte API-Kosten.
- [ ] **Facebook-/Instagram-Werbekampagne (Session „Facebook-Werbekampagne", Stand 2026-08-26)** — Kampagne `LinguaFlow – Traffic Test August 2026` liegt seit dem 16.08. als **Entwurf** im neuen Werbekonto „LinguaFlow App" (`1724098535586816`): Traffic, 10 €/Tag, 16.–23.08., AT/DE/CH, Interessen Fremdsprache + Fremdsprachenunterricht, Ziel-URL mit UTM-Parametern gesetzt. **Achtung: der eingestellte Zeitraum 16.–23.08. ist abgelaufen und muss neu gesetzt werden.** **Nächster Schritt:** verifizierte Telefonnummer hinterlegen (Meta-Fehler `#3858013`, blockiert die Auslieferung), dann Werbebild per Drag & Drop in Anzeige A, Texte A/B/C eintragen, veröffentlichen. Voller Stand inkl. Anzeigentexte, Bild-Rezept und Stolperfallen: `.claude/features/facebook-werbekampagne.md`. **Nicht ohne Rückfrage veröffentlichen** (kostet ab Klick echtes Geld).
- [ ] **JSON-LD nachrüsten für die restlichen Seiten** (optional, ~7,25 €) — erledigt sind `hilfe.html` und die drei Cluster-Seiten. Offen mit noch deutschem Schema: `index.html` (5 Blöcke) sowie `ratgeber.html` und die vier Ratgeber-Artikel (je 1 Block), zusammen 362.736 Zeichen. Vorgehen wie bei hilfe.html: `gh workflow run "Retrofit JSON-LD (DeepL)" -f files="index.html ratgeber.html …"`, vorher mit `-f dry_run=true` prüfen. `was-ist-neu.html`, `newsletter.html` und `funktionen.html` haben kein JSON-LD und brauchen nichts

- [ ] **Kleinigkeit: Eigenname „anabin" fehlt in der englischen Fassung** — DeepL hat den Kartentext auf `/en/zurueck-in-deinen-beruf/pflege/` umformuliert und den Eigennamen dabei fallen lassen (in TR und UK blieb er erhalten). Unkritisch, weil es ein Verweistext ist und kein Faktum; bei Gelegenheit über `POST_TRANSLATION_FIXES` absichern

- [ ] **DMARC-Record erweitern** (WordPress DNS) — von `p=none` auf `p=quarantine`, siehe `.claude/features/dmarc-erweitern.md` (aus dem alten Ordner gerettet, Stand Juni 2026 — Status vor Umsetzung prüfen)

- [ ] **Bild-alt-Texte und Lightbox-Beschriftungen sind in ALLEN Sprachversionen deutsch** — die sichtbaren `<h3>`/`<p>` der Galerie sind übersetzt, aber `alt="…"` und `title`/`desc` im `slides`-Array blieben deutsch (z. B. `/es/`: „Übersetzungsverlauf – gespeicherte Dekodierungen…"). Betrifft SEO und Screenreader; beim Fix `adjust_html()` in translate-site.py prüfen

## Offen — Marketing / Sichtbarkeit (aus altem Ordner übernommen, siehe `.claude/features/marketing-backlog.md`)
- [ ] App-Verzeichnisse eintragen (Product Hunt, AlternativeTo, Capterra, G2, …) — 3–5 h, hoher Impact
- [ ] Free Tool auf der Domain (z. B. Wort-für-Wort-Demo ohne App-Download) — 5–15 h, sehr hoher Impact
- [ ] Blog-Sektion starten — laufend, hoher Impact

## Erledigt
- Newsletter-Anmeldeseite `/newsletter.html` (DE+EN, `DE_EN_ONLY_FILES`): eigenes Formular im Website-Stil, sendet an MailerLite-Embedded-Form-Endpunkt (Account 2515300, Formular 194891825918511013, Gruppe `website`, Double-Opt-in AN, Honeypot, Datenschutz-Link). Footer-Link „Newsletter" in index.html + hilfe.html (Quelle + alle 33 Sprachordner per Skript, Sprachordner verlinken auf /en/newsletter.html, Cache-Trick → 0 € für Bestandsseiten). LIVE + End-to-End getestet 2026-08-04 (Test-Abonnent kam als `unconfirmed` in Gruppe `website` an, danach per API restlos gelöscht)
- „Was ist neu"-Seite (Changelog): `/was-ist-neu.html` neu erstellt — Zeitleiste aller App-Versionen (2.4, 2.3.1, 2.3, 2.0, 1.0, neueste zuerst), rekonstruiert aus Git-Verlauf + Release-Notes des iOS-Projekts. Nur DE+EN (`DE_EN_ONLY_FILES` in translate-site.py). LIVE seit 2026-08-04 (DE + /en/ beide 200 OK). Offen: Verlinkung von index/hilfe (per Cache-Trick fast kostenlos möglich)
- Rechtstexte (Datenschutz, EULA, Impressum) nur noch Deutsch + Englisch: `LEGAL_FILES` + `languages_for()` in translate-site.py, 96 Sprachversionen durch Redirect-Stubs auf /en/ ersetzt, Hinweissatz „deutsche Fassung verbindlich" ergänzt. Spart ~10 €/Datei bei künftigen Änderungen (nur noch EN wird übersetzt, ~30 Cent)
- GSC-Indexierungs-Fixes: 27 Redirect-Stubs für alte WordPress-URLs (z. B. /kontakt/ → /hilfe.html), alle `href="index.html"`-Links → `href="./"` (310 Dateien, Quelle + Übersetzungen per sed), eigene 404.html. Translation-Cache-Hashes manuell aktualisiert → KEINE DeepL-Neu-Übersetzung nötig/ausgelöst
- 30-Tage-Testzeitraum (nur Jahresabo, iOS + Android) auf der Website: Preiskarte „Jährlich" in index.html (Feature-Zeile + Transparenzhinweis: gilt für neue Abonnenten, automatischer Übergang ins bezahlte Abo) + neue FAQ „Gibt es einen kostenlosen Testzeitraum?" in hilfe.html (sichtbar + JSON-LD)
- Ratgeber-Bereich Phase 1: Hub (`ratgeber.html`) + 4 Artikel (Anerkennung, 24h-Pflege→Festanstellung, Deutsch B2 Pflege, Haushaltshilfe Wien), automatisch in 33 Sprachen übersetzt
- „Ratgeber"-Links in Navbar + Footer von index.html und hilfe.html
- 404-Fix: AGB/Widerruf-Footer-Links auf absolute Pfade (`/agb.html`, `/widerruf.html`) — vorher 404 in allen Sprachversionen
- Kosten-Bug im Übersetzungs-Skript gefixt: `enrich_source_file()` ließ die Einrückung vor den i18n-Markern bei jedem Lauf wachsen → Datei-Hash änderte sich → JEDER Workflow-Lauf übersetzte die komplette Website neu (~17 €/Lauf)

## Backlog (Ratgeber Phase 2 — je Artikel einmalig ~15 € DeepL)
- Geringfügige Jobs in Österreich (Grenzen, Regeln)
- Putzfrau-Verdienst-Vergleich (privat vs. Firma vs. Plattform)
- Pflegeassistenz-Ausbildung in Österreich (Weg, Kosten, Stipendium)
- Gesundheitsberuferegister: Registrierung Schritt für Schritt
- Bewerbung auf Deutsch (Lebenslauf, Vorstellungsgespräch)
- Agentur-Vergleich 24-Stunden-Betreuung
- SK/RO-Feinschliff: Wenn /sk/ und /ro/ Traffic zeigen, Titles/H1 dort manuell auf idiomatische Suchbegriffe anpassen („opatrovateľka", „îngrijitoare")

## Nach dem Launch (manuell zu erledigen)
- Google Search Console: Sitemap neu einreichen, Indexierung von /ratgeber.html + Artikeln anfordern
- Nach 4–8 Wochen: Rankings/Klicks prüfen (v. a. /sk/ und /ro/), dann Phase 2 entscheiden

## Änderungsprotokoll
- 2026-08-26 (Nachtrag zum 16.08.): **Galerie-Erweiterung ist inzwischen in allen Sprachen live** — durch den Uebersetzungslauf vom 21.08. haben DE, EN und alle uebrigen Sprachversionen die sieben Kacheln (stichprobenartig geprueft: es, fr, ja, ar, pl). Die beiden damals offenen Punkte (EN-Freigabe, restliche 31 Sprachen) sind damit erledigt und wurden aus `In Arbeit` entfernt. `funktionen.html` ist ebenfalls uebersetzt (Cache-Eintrag vorhanden, /en/ liefert 200). Weiterhin offen bleibt: alt-Texte und Lightbox-Beschriftungen sind in allen Sprachversionen deutsch (auf /es/ gegengeprueft).
- 2026-08-21 (**LIVE**): **JSON-LD von `hilfe.html` in allen 33 Sprachen nachgerüstet — für 7,35 € statt 25,95 €.** Der naheliegende Weg (Cache-Eintrag löschen, neu übersetzen lassen) hätte den kompletten Seitentext mitübersetzt: 929.940 Zeichen sichtbarer Text gegenüber nur 367.323 Zeichen Schema. Stattdessen neu: `scripts/retrofit-jsonld.py` ersetzt **ausschließlich** den JSON-LD-Block in den vorhandenen Sprachdateien und lässt den bereits übersetzten Fließtext unangetastet. Ergebnis: 33 Dateien aktualisiert, 0 übersprungen, Verbrauch 868.764 → 1.236.087 Zeichen = **367.323 Zeichen, exakt wie im Trockenlauf vorhergesagt**. Verifiziert: alle 33 Sprachversionen valide, je 28 FAQ-Einträge erhalten, **keine einzige noch auf Deutsch**; im Diff kein einziger sichtbarer HTML-Text (7.557 Zeilen hinzu, 7.557 entfernt — reiner Blocktausch). Live geprüft in EN, ES, JA, AR.
- 2026-08-21: **Eigener Workflow „Retrofit JSON-LD (DeepL)"** (`.github/workflows/retrofit-jsonld.yml`), ausschließlich per `workflow_dispatch` — ein Lauf kostet Geld und soll nie versehentlich durch einen Push starten. Eingaben: Dateiliste und `dry_run`. Der Trockenlauf wurde vor dem echten Lauf einmal mit dem echten Schlüssel durchgeführt und meldete dieselbe Zeichenzahl. Sicherungen im Skript: Blockzahl in Quelle und Ziel muss übereinstimmen, jeder Block wird nach dem Ersetzen mit `json.loads` gegengeprüft, und fällt `translate_jsonld_block()` auf das deutsche Original zurück, bleibt die Datei unverändert statt halb übersetzt.
- 2026-08-21 (**LIVE**): **JSON-LD-Übersetzung ist ausgerollt.** Workflow-Lauf erfolgreich, 12 Übersetzungen, **keine einzige Rückfall-Meldung** — die Schema-Übersetzung lief in allen 12 Fällen sauber durch. Verbrauch 548.880 → 868.764 Zeichen, also **319.884 Zeichen ≈ 6,40 €** (Trockenlauf hatte 9,88 € geschätzt). Live verifiziert: alle 12 URLs antworten mit 200, **JSON-LD in allen 12 valide**, alle fünf Schema-Typen vorhanden, je 12 FAQ-Einträge erhalten, `inLanguage` je Sprache korrekt, `Organization.name` überall „LinguaFlow e.U.", Inline-JavaScript in allen geprüften Dateien fehlerfrei. Beispiel `/en/zurueck-in-deinen-beruf/pflege/`: headline „German for Certification as a Registered Nurse …", FAQ[0] „What level of German do I need to be certified as a nurse?", HowTo[0] „Submit an application to the state office".
- 2026-08-21: **Beobachtung ohne Handlungsbedarf:** Im arabischen Schema steht „المادة 16د" — DeepL hat das „d" aus „§ 16d" ins arabische „د" transliteriert, während der sichtbare Text weiterhin „16d" schreibt. Inhaltlich korrekt („Artikel 16d"), nur eine kleine Inkonsistenz zwischen Fließtext und Schema. Bewusst nicht korrigiert: Der Nutzen wäre marginal, ein weiterer Übersetzungslauf kostet rund 6 €. Falls es später stören sollte, ließe es sich über `POST_TRANSLATION_FIXES` erledigen — die Regeln greifen auf das fertige HTML und damit auch auf den Schema-Block. Alle übrigen Eigennamen (telc 14×, BAMF 2×, TVöD 1×) stehen im Schema aller vier Sprachen unverändert.
- 2026-08-21: **JSON-LD wird jetzt mitübersetzt** (`translate_jsonld_block()` + `deepl_translate_texts()` in `translate-site.py`). Bisher schützte `deepl_translate()` ALLE `<script>`-Blöcke pauschal per Platzhalter — nötig gegen DeepLs Escaping, aber die Folge war ein deutsches Schema in jeder übersetzten Seite. Neu werden die beiden Fälle getrennt: **Inline-JavaScript bleibt unangetastet** (dort riskiert jede Änderung einen SyntaxError), **JSON-LD wird geparst** und nur in den Feldern `name`, `text`, `description`, `headline` übersetzt. Struktur, URLs, `@type`, `datePublished` und `position` bleiben unverändert; `Organization`-Knoten werden ausgelassen, damit „LinguaFlow e.U." nicht übersetzt wird; `inLanguage` wird auf die Zielsprache gesetzt. Die Texte gehen gebündelt an DeepL (bis zu 50 pro Request) — 43 FAQ- und HowTo-Texte kosten so **einen** Aufruf statt 43.
- 2026-08-21: **Rückfallebene für die JSON-LD-Übersetzung.** Bei jedem Problem wird der unveränderte deutsche Originalblock eingesetzt: unlesbares JSON im Eingang, Fehler beim DeepL-Aufruf, abweichende Antwortlänge, oder ungültiges JSON nach dem Zusammenbauen (`json.dumps` + `json.loads`-Gegenprobe). Begründung: Ein deutsches Schema ist unschön, ein kaputtes wäre ein kritischer Fehler in der Google Search Console. Alle vier Fehlerfälle plus der Normalfall wurden vor dem Push einzeln getestet.
- 2026-08-21: **Cluster-Seiten gezielt neu übersetzt.** Damit die drei Seiten das übersetzte Schema tatsächlich bekommen, wurden **nur ihre drei Cache-Einträge** entfernt (17 → 14). `SCRIPT_VERSION` bewusst NICHT erhöht — das hätte alle Dateien invalidiert. Trockenlauf vorab: 12 Übersetzungen, 425.172 Zeichen sichtbarer Text plus 68.664 Zeichen Schema-Felder, zusammen ≈ 9,88 €. Verifiziert: JSON in allen vier Sprachen valide, 12 FAQ-Einträge erhalten, `inLanguage` korrekt, `Organization.name` unverändert, Inline-JavaScript in allen geprüften Dateien fehlerfrei.
- 2026-08-21: **Qualitätsprüfung der neuen Übersetzungen.** (a) **Eigennamen unversehrt**: telc (55×), BAMF (11×), TVöD (7×), ZAB, § 16d, P7 und die 75-Punkte-Schwelle stehen in EN, TR, UK und AR jeweils genauso oft wie im deutschen Original — DeepL hat sie nicht verfälscht. (b) **Nav-Label passt in allen vier Sprachen**: Recognition, Onay, Визнання, الاعتماد — alle kurz genug, kein Umbruch. (c) **Arabisch ohne `dir="rtl"`** — das ist allerdings der Zustand aller 34 arabischen Seiten im Repo, nicht neu durch diese Arbeit; bewusst nicht angefasst, weil es alle Bestandsseiten beträfe. (d) **Befund JSON-LD deutsch** — siehe offener Punkt oben.
- 2026-08-21 (**LIVE**): **Alles committet und gepusht, Workflow durchgelaufen.** Fünf Commits auf `main`: (1) `e500dbb` JS-Escaping-Fix in 235 Sprachdateien plus `funktionen.html` — Arbeit aus den Sessions vom 05./06.08., die uncommittet im Arbeitsverzeichnis lag; (2) `71bfa36` die drei neuen Seiten, llms.txt, Nav-Eintrag, Ratgeber-Karte, Pipeline-Erweiterung; (3) `f672f1e` Enrichment-Zustand von `funktionen.html` stabilisiert; (4) `ef70b67` POST_TRANSLATION_FIXES. Vor dem Push war ein Rebase auf einen Bot-Commit nötig (Konflikt nur in `sitemap.xml`, durch Neugenerierung gelöst). Übersetzungs-Workflow: **erfolgreich in 1m32s, 46 Übersetzungen, 11 Cache-Hits** — exakt wie simuliert. Alle 10 geprüften URLs live mit 200, darunter `/tr/zurueck-in-deinen-beruf/pflege/` und `/ar/zurueck-in-deinen-beruf/akademiker/`. Stichprobe live: türkische H1 übersetzt, canonical korrekt, Nav-Eintrag zeigt in `/tr/` auf die türkische und in `/es/` auf die deutsche Fassung.
- 2026-08-21: **Kosten deutlich niedriger als geschätzt: rund 9,24 € statt 37 €.** DeepL-Verbrauch laut Workflow-Log 86.641 → 548.880 Zeichen, also **462.239 Zeichen**. Die Vorab-Simulation kam auf 1.854.519 Zeichen, weil sie `deepl_translate()` gemockt hat — also *vor* der Platzhalter-Ersetzung der `<script>`-Blöcke gemessen wurde. Gerade die neuen Seiten haben sehr große JSON-LD-Blöcke (Article + FAQPage + HowTo + Organization), die gar nicht an DeepL gehen. **Merke für künftige Kostenschätzungen:** nicht `deepl_translate` mocken, sondern `deepl_translate_raw` — sonst überschätzt man den Verbrauch um ein Vielfaches.
- 2026-08-21: **Polnisches Keyword dauerhaft abgesichert** (`POST_TRANSLATION_FIXES` in `translate-site.py`). Der Übersetzungslauf hatte die Handarbeit vom 18.08. wie vorhergesagt überschrieben: DeepL machte aus der Birkenbihl-App ein „nauka języków z aplikacją Birkenbihla" — sprachlich richtig, aber ohne das in Polen gesuchte „metoda". Statt erneuter Handarbeit gibt es jetzt eine Tabelle sprachspezifischer Korrekturen, die `adjust_html()` nach jeder Übersetzung anwendet; der Regex greift die H2 über das Wort Birkenbihl und ist damit unabhängig von DeepLs Formulierung. Idempotenz geprüft. `pl/index.html` zusätzlich direkt korrigiert, weil `index.html` im Cache steht. **Live verifiziert.**
- 2026-08-21: **`linguaflow-briefing.md` in `.gitignore` aufgenommen.** Beim Commit-Vorbereiten aufgefallen: Das Repository ist **öffentlich** (`visibility: public`), das Briefing enthält aber Preismodell, geplante Preisänderungen, Marktanalyse und Wettbewerbsbewertung. Es liegt weiterhin lokal im Projektordner, kann aber nicht mehr versehentlich mitcommittet werden.
- 2026-08-21: **Übersetzungs-Pipeline für Seiten in Unterverzeichnissen erweitert** (`scripts/translate-site.py`). Das Skript sprach Quelldateien bisher ausschließlich über den flachen Dateinamen im Wurzelverzeichnis an (`source_path.name`), an 13 Stellen mit der Sonderfall-Abfrage `filename == "index.html"`. Neu sind zwei Helfer: `rel_key()` liefert den Pfad relativ zur Repo-Wurzel als Kennung (für Wurzeldateien identisch zum bisherigen Dateinamen → **Cache bleibt gültig, keine Neuübersetzung**), `url_path()` bildet daraus den URL-Pfad (`index.html` → `""`, `zurueck-in-deinen-beruf/index.html` → `zurueck-in-deinen-beruf/`). Damit stimmen canonical, og:url, hreflang, Sitemap und beide Sprachumschalter auch für Unterverzeichnisse. Ausgabe legt Verzeichnisse mit `parents=True` an. **`SCRIPT_VERSION` bewusst bei „4" belassen.**
- 2026-08-21: **Cluster-Seiten werden in vier Sprachen übersetzt: EN, TR, UK, AR** (`CLUSTER_LANG_CODES`), nicht in alle 33. Begründung aus dem Strategiebriefing: Englisch als Verkehrssprache plus die Herkunftssprachen der Zielgruppe — Türkei (9.200 Anerkennungen 2024), Ukraine (6.400) und Syrien (4.300) gehören zu den häufigsten Herkunftsländern. Japanisch oder Finnisch wären für „Anerkennung als Pflegefachkraft in Deutschland" verschenktes Geld: 33 Sprachen hätten rund 87 € gekostet, vier kosten rund 10 €. Erweitern ist ein Einzeiler in `CLUSTER_LANG_CODES`.
- 2026-08-21: **Neue Regel „Cluster-Links auf die Sprachversion umbiegen"** (`adjust_html`, Schritt 2c). Interne Links wie `/zurueck-in-deinen-beruf/pflege/` werden zu `/tr/zurueck-in-deinen-beruf/pflege/` — **aber nur für Sprachen, die es dort auch gibt** (`CLUSTER_SLUGS`). Ohne diese Einschränkung würde der Nav-Eintrag in `/es/index.html` auf eine nicht existierende spanische Cluster-Seite zeigen (404); so bleibt er dort auf der deutschen Fassung. Verifiziert: tr/uk/ar/en zeigen auf ihre Sprachversion, es/ja/pl auf die deutsche.
- 2026-08-21: **Enrichment-Instabilität der Cluster-Seiten gefunden und umgangen.** `enrich_source_file()` erzeugte auf den neuen Seiten bei Lauf 1 und Lauf 2 unterschiedliche Ergebnisse (die Einrückung vor `<!-- i18n-navswitcher:start -->` verschwand im zweiten Lauf) — der Datei-Hash hätte sich damit bei **jedem** Workflow-Lauf geändert und jedes Mal ~10 € Neuübersetzung ausgelöst. Ursache: Der Switcher erbt beim Einfügen die Einrückung des `<button class="hamburger">`, beim Entfernen frisst `[ \t]*` sie wieder mit. Der Zustand **konvergiert nach dem zweiten Lauf**; die drei Quelldateien wurden deshalb lokal in den stabilen Zustand überführt und so committet (dieselbe Vorgehensweise wie 2026-08-04 bei `was-ist-neu.html`). **Am Enrichment-Mechanismus selbst wurde bewusst nicht geschraubt** — eine Änderung dort würde die Hashes aller Bestandsdateien gefährden (~70 € Voll-Neuübersetzung).
- 2026-08-21: **Trockenlauf statt Blindflug.** Vor dem Push wurde der komplette Workflow zweimal in einer Repo-Kopie mit gemocktem DeepL simuliert. Ergebnis mit echtem Cache: **46 Übersetzungen, 1.854.519 Zeichen, ~37 €** (index.html 33 Sprachen ~27 €, die drei Cluster-Seiten 12 Übersetzungen ~10 €, funktionen.html 1 Sprache ~0,20 €). Regressionstest gegen den Bestand: canonical, og:url, hreflang-Anzahl (35) und Switcher-Optionen (68) in `es/hilfe.html`, `es/index.html`, `ja/ratgeber.html`, `en/datenschutz.html` **unverändert**; keine der deutschen Bestands-Quelldateien wurde von `enrich_source_file()` angefasst; 11 Cache-Schlüssel unverändert, neu nur die drei Cluster-Pfade und `funktionen.html`.
- 2026-08-21: **Drei neue Seiten „Zurück in deinen Beruf" (AEO-Cluster, Deutschland)** aus `linguaflow-briefing.md` gebaut: `zurueck-in-deinen-beruf/index.html` (Hub), `.../pflege/index.html`, `.../akademiker/index.html`. Ziel laut Briefing Abschnitt 3 ist **Zitierbarkeit durch ChatGPT und Perplexity**, nicht Google-Ranking: jede H2 ist eine echte Nutzerfrage, der Absatz darunter beantwortet sie vollständig ohne Vorrede, Absätze sind selbsttragend, Fakten stehen in Tabellen, jede Zahl mit Quelle und Jahr, „Stand: August 2026" sichtbar oben. Fließtext 1.597 / 1.982 / 1.619 Wörter (Zielkorridor 1.500–2.500; die Gesamtwortzahl liegt höher, weil Tabellen, FAQ, Inhaltsverzeichnis und Quellenliste dazukommen). Schema je Seite: **Article + FAQPage (je 12 Fragen) + HowTo (je 6 Schritte) + Organization + BreadcrumbList**, alle JSON-LD-Blöcke gegen `json.loads` validiert. **FAQ bewusst offen sichtbar** (H3 + Absatz) statt im Akkordeon der Bestandsseiten — versteckter Text ist für Antwortmaschinen schlechter zitierbar. Tabellen-/Callout-Styles inline pro Seite (wie `.toc` in den Ratgeber-Seiten), `styles.css` **nicht** angefasst. Verifiziert: 1× H1 je Seite, H-Reihenfolge ohne Sprünge, alle 30 internen Anker treffen ein Ziel, alle internen Links 200, **alle 28 externen Quellenlinks 200**, kein horizontaler Seiten-Overflow bei 1280 px und 375 px (breite Tabellen scrollen innerhalb ihres Containers).
- 2026-08-21: **Faktenprüfung gegen die Sperrliste (Briefing 5.14)** — systematisch geprüft, ein echter Verstoß gefunden und behoben: Die Seite Pflege nannte an vier Stellen „ca. 2,5–4 Monate" als Vorbereitungsdauer der Kenntnisprüfung (so steht es in Briefing 5.4), **Abschnitt 5.14 verbietet genau diese Angabe** („keine offizielle Statistik"). Das Briefing widerspricht sich hier selbst; laut Auftrag hat die Sperrliste Vorrang. Ersetzt durch „Prüfung an einem Termin; zur Vorbereitungsdauer gibt es keine offizielle Statistik". Alle übrigen Sperrpunkte sauber: ZAB-Gebühr steht mit **208 €** (nicht 200 €), ZAB-Bearbeitungsengpass wird ausdrücklich genannt, die Bayern-Quote von 52 % kommt nicht vor (stattdessen „rund ein Drittel"), es wird kein BAMF-Spezialkurs „Recht" behauptet (im Gegenteil: ausdrücklich verneint), keine Bestehensquote der Kenntnisprüfung **Pflege**, keine Abbruchquoten, keine Defizitbescheid-Frist, keine einheitliche telc-Gebühr, Bundesland-Matrix nur mit „ohne Gewähr". Keine einzige Zahl ohne Beleg im Briefing.
- 2026-08-21: **robots.txt geprüft (Auftragspunkt 1) — kein Handlungsbedarf.** Live-Datei ist `User-agent: *` / `Allow: /` **ohne jede `Disallow`-Regel**; GPTBot, OAI-SearchBot, ChatGPT-User, PerplexityBot, Perplexity-User, ClaudeBot, Claude-SearchBot, Google-Extended und Applebot-Extended sind damit alle erlaubt. Bewusst **keine** einzelnen `User-agent:`-Blöcke ergänzt: Für einen namentlich genannten Bot gilt in robots.txt ausschließlich sein eigener Block, der `*`-Block wird für ihn ignoriert — explizite Blöcke brächten hier null Gewinn und wären eine neue Fehlerquelle.
- 2026-08-21: **`llms.txt` neu angelegt** (Auftragspunkt 2, Ersatz für die Yoast-Funktion). Yoast/WordPress existiert für linguaflow.app nicht mehr, deshalb als statische Datei im Repo: Kurzbeschreibung der App, die drei neuen Seiten mit inhaltlicher Zusammenfassung, Ratgeber, App-Seiten, Rechtstexte, dazu ein Hinweisblock für Antwortmaschinen (die neuen Seiten betreffen **Deutschland**, die Ratgeber **Österreich** — beim Zitieren das Land nennen). Alle 16 verlinkten Pfade gegen das Dateisystem geprüft.
- 2026-08-21: **Navigationseintrag + interne Verlinkung.** (a) `index.html`: neuer Menüpunkt zwischen „Sprachen" und „Preise", Pfad **absolut** `/zurueck-in-deinen-beruf/` — ein relativer Pfad hätte in den Sprachordnern auf `/es/zurueck-in-deinen-beruf/` gezeigt (404), da `adjust_html()` nur Asset-Pfade absolut macht; per Simulation des `/es/`-Laufs verifiziert. **Label „Anerkennung" statt „Zurück in deinen Beruf"**: Das lange Label ließ den Menüpunkt schon bei 1298 px auf zwei Zeilen umbrechen (Höhe 52 statt 26 px); mit dem kurzen Label ist die Navbar bis hinunter zu **800 px** (Hamburger greift erst bei ≤768 px) umbruchfrei und ohne Overflow. „Anerkennung" ist zugleich der Suchbegriff der Zielgruppe; im Footer der neuen Seiten steht weiterhin „Zurück in deinen Beruf". (b) `ratgeber.html`: fünfte Karte mit ausdrücklicher Deutschland-Kennzeichnung, weil der Ratgeber sonst Österreich behandelt. (c) Hub ↔ Unterseiten gegenseitig verlinkt (Pillar-Cluster).
- 2026-08-21: **Kostenentscheidung Übersetzung** (Christian, in Kenntnis der Folge für Polnisch bestätigt): `index.html` wird **voll übersetzt** (33 Sprachen, ~27 €) statt Cache-Trick — der Menüpunkt erscheint damit übersetzt in allen Sprachversionen, führt aber vorerst auf die deutschen Seiten. Für `ratgeber.html` **Cache-Trick** angewandt (0 €): `enrich_source_file()` vorher auf Idempotenz geprüft (zwei Läufe, byte-identisch), dann Hash im stabilen Zustand in `scripts/.translation-cache.json` gesetzt. Simulation des nächsten Workflow-Laufs bestätigt: nur `index.html` (33 Sprachen) und `funktionen.html` (1 Sprache, vorbestehender offener Punkt aus anderer Session) werden übersetzt, alle übrigen 11 Quelldateien sind Cache-Hits.
- 2026-08-21: **Sitemap**: `scripts/translate-site.py` um `EXTRA_SITEMAP_URLS` erweitert, damit die drei neuen URLs den nächsten Workflow-Lauf überleben — `build_sitemap()` erzeugt die Datei komplett neu aus `SOURCE_FILES`, die neuen Seiten liegen aber in eigenen Verzeichnissen und werden davon nicht erfasst. **`SCRIPT_VERSION` bewusst unverändert** (eine Erhöhung würde den Cache invalidieren, ~70 € Voll-Neuübersetzung). Sitemap jetzt 253 statt 248 URLs: 3 neue Seiten plus `funktionen.html` in DE und EN, das ohnehin beim nächsten Lauf dazugekommen wäre.
- 2026-08-18: **H2-Überschriften mit Keyword „Birkenbihl" ergänzt** (SEO). Analyse zuvor: Das Keyword „birkenbihl sprachen app" stand nur im `<title>` und in einer H3 („Die Lösung: Birkenbihl-inspiriertes Lernen"), in keiner H1/H2. (a) `index.html` Z. 316: H2 „Schluss mit dem alten Schulsystem." → „Schluss mit dem alten Schulsystem – Sprachen lernen mit der Birkenbihl-App". (b) `pl/index.html` Z. 325: H2 „Koniec ze starym systemem szkolnym." → „Koniec ze starym systemem szkolnym – aplikacja do nauki języków metodą Birkenbihla" (polnisches Keyword „metoda Birkenbihl", das laut Christian in der Suche stark gefunden wird; DeepL würde aus der deutschen Fassung „z aplikacją Birkenbihl" machen, also OHNE „metoda"). **Achtung Repo-Regel:** die PL-Datei wurde bewusst von Hand bearbeitet — sie wird beim nächsten `index.html`-Übersetzungslauf überschrieben, wenn nicht der Cache-Trick greift. Noch NICHT gepusht. Nebenbefund (nicht angefasst): die `<meta name="description">` ist in ALLEN Sprachordnern noch deutsch. **Nachtrag (Entscheidung Christian):** Cache-Trick statt Voll-Übersetzung — `scripts/.translation-cache.json` für `index.html` von `ddb318f1…` auf `95c4e8fb…` gesetzt. Wirkung: Der Workflow überspringt `index.html`, die polnische Handarbeit überlebt, die übrigen 32 Sprachen behalten ihre alte H2. Kosten 0 € statt ~27 €. Verifiziert durch lokale Simulation: (a) `enrich_source_file()` ändert `index.html` nicht mehr (stabiler Zustand, Hash bleibt), (b) Cache-Abgleich über alle `SOURCE_FILES` meldet Cache-Hit für `index.html`. **Nebenbefund:** `funktionen.html` hat weiterhin keinen Cache-Eintrag und wird beim Push mit-übersetzt (~0,17 €, bekannter offener Punkt, nicht durch diese Änderung verursacht)
- 2026-08-16: **App-Screenshots erneuert + Galerie um 2 Funktionen erweitert** (Anlass: erste Facebook-Werbekampagne führt auf linguaflow.app, die Galerie zeigte noch den Stand von Juli). (a) Alle 5 Bestandsbilder ersetzt — Startseite/Bücher/Reader aus Christians Simulator-Aufnahmen vom 15.08., Dekodierung/Verlauf aus den Roh-Screenshots vom 09.08.; einheitlich auf 1200 px Breite skaliert, WebP mit `cwebp -q 82`. Bilder liegen zentral in `img/`, wirken dadurch **ohne** Übersetzungslauf in allen 34 Sprachversionen. (b) Zwei neue Galerie-Kacheln „Lernweg" und „Text generieren" (Quelldateien: `screenshot-lernweg`, `screenshot-textgen`) — vorerst **nur DE + EN + EN-GB** (Christians Entscheidung, restliche Sprachen später). (c) **Cache-Trick**: `scripts/.translation-cache.json` für `index.html` auf den neuen SHA-256 gesetzt → der Workflow überspringt die Datei, spart ~27 € DeepL; die englischen Versionen wurden dafür ausnahmsweise von Hand erweitert und werden beim nächsten regulären Lauf sauber überschrieben. Verifiziert: alle 3 Dateien mit `node`+`vm` auf JS-Syntax geprüft (7 valide `slides`-Einträge), 7 sichtbare Kacheln gleicher Größe im Browser gemessen, alle 7 Bilder laden (1200×2607–2609), div-Balance unverändert (122/122). **Wichtig gelernt:** Die 33 Sprachordner waren zuerst versehentlich von Hand bearbeitet worden (Verstoß gegen die Repo-Regel) — vollständig zurückgesetzt, byte-identisch mit dem Stand davor, die uncommittete JS-Reparatur vom 06.08. blieb dabei unversehrt
- 2026-08-06: **JS-Escaping-Bug behoben** (Schritt 0 des Website-Updates, Plan in ~/.claude/plans/temporal-hopping-simon.md): DeepL escaped Inline-`<script>`-Inhalte trotz `ignore_tags=script` (`&#x27;` statt `'`) → SyntaxError in ALLEN 33 Sprachversionen; Mobile-Menü, Lightbox und das EN-Newsletter-Formular waren funktionsunfähig. Fix (a) `translate-site.py`: Platzhalter-Schutz jetzt für ALLE `<script>`-Blöcke (vorher nur JSON-LD) — spart nebenbei ~25 % Zeichen pro API-Call; (b) `adjust_html()` Schritt 2b: Bildpfade in JS-Strings (Lightbox-`slides`) werden absolut; (c) einmaliger lokaler Reparatur-Durchlauf über 236 bestehende Sprachdateien (0 € — kein DeepL-Aufruf, Cache unangetastet, Redirect-Stubs unangetastet). Verifiziert: 0 kaputte Script-Blöcke übrig, Platzhalter-Roundtrip byte-identisch. Noch NICHT gepusht — geht mit dem gebündelten Website-Update live
- 2026-08-04 (spät, VOLLTEST ✅): Christian hat den kompletten Durchlauf mit einer „Hide My Email"-Relay-Adresse gemacht: Verbergen-Option gewählt → DOI-Mail kam an der Relay-Adresse an (Apple-Relay-Registrierung wirkt) → Klick → Abonnent `active` in Gruppe `website` (per API bestätigt). Erster echter Website-Abonnent. Hinweis: Bestätigungsseite + DOI-Mail sind MailerLites englischer Standard (Anpassung = Premium-Tarif)
- 2026-08-04 (spät, ✅ GELÖST): „E-Mail-Adresse verbergen" erscheint jetzt auf iPhone UND Mac (Screenshots von Christian). Es brauchte BEIDES: (a) die Formular-Signale auf der Seite — zweites unsichtbares E-Mail-Feld (gegen Login-Fehlklassifizierung), Abo-Vokabular, verstecktes `autocomplete="new-password"`-Feld (Registrier-Signal) — und (b) auf Christians Geräten die Safari-Einstellung „Autom. ausfüllen → Infos von Kontakten", die ausgeschaltet war (jetzt iPhone + Mac an). Alle drei Seiten-Maßnahmen bleiben drin — sie sorgen dafür, dass Besucher mit korrekten Einstellungen die Option zuverlässig angeboten bekommen. MERKE für Support-Anfragen: Ohne „Infos von Kontakten" (Einstellungen → Safari → Autom. ausfüllen) zeigt Safari die Verbergen-Option NICHT, egal wie das Formular gebaut ist
- 2026-08-04 (spät, 3. Anlauf Hide My Email): Abo-Vokabular allein reichte nicht (iPhone-Test: weiterhin nur echte E-Mail). Eskalation umgesetzt: verstecktes `autocomplete="new-password"`-Passwortfeld im Offscreen-Container — das härteste Registrier-Signal für Safaris Formular-Klassifikator. Feld bleibt leer, wird nicht mitgesendet, kein Save-Prompt (der erscheint nur bei befülltem Passwortfeld). Falls auch das nicht greift: Gerätediagnose (Einstellungen → Safari → Autom. ausfüllen; Test auf bekannt funktionierender Registrierungsseite), danach ggf. Apple-Feedback
- 2026-08-04 (spät, 2. Anlauf Hide My Email): Christians iPhone-Test nach dem Zwei-Felder-Fix: Safari schlägt jetzt die echte E-Mail vor (Login-Fehlklassifizierung behoben), aber noch kein „E-Mail-Adresse verbergen" — das bietet Safari nur bei Registrier-Kontext an. Vermutete Ursache: deutsches „anmelden" = Safari-Login-Vokabular (EN-Seite sagt „Sign up now"). Deutsche Texte auf Abo-Vokabular umgestellt („Jetzt abonnieren", „Abonnement aktiv/bestätigen"). Falls das nicht reicht, nächste Eskalation: verstecktes `autocomplete="new-password"`-Feld als hartes Registrier-Signal
- 2026-08-04 (spät): „E-Mail-Adresse verbergen" (Apple Hide My Email) auf newsletter.html ermöglicht: Safari stuft Formulare mit nur EINEM E-Mail-Feld als Login-Feld ein und bietet dann Passwörter statt der Verbergen-Option an (bekannter Safari-Quirk, Apple-Dev-Forum #716656). Fix: zweites unsichtbares E-Mail-Feld (`email_confirm`, offscreen wie der Honeypot, wird nicht mitgesendet) → Safari erkennt „neue E-Mail"-Kontext. Außerdem `novalidate` entfernt (native Browser-Validierung, Apple-AutoFill-Empfehlung). Verifikation auf echtem iPhone mit iCloud+ steht aus (im Simulator ohne iCloud-Konto nicht testbar)
- 2026-08-04 (abends, Nachtrag): Workflow-Trigger-Lücke gefixt: im `paths`-Filter von translate-site.yml fehlten `was-ist-neu.html` und `newsletter.html` — Pushes, die NUR diese Dateien änderten, lösten keine Übersetzung aus (so blieb der CSS-Fix zunächst ohne EN-Update; aufgefallen, weil der zweite Push keinen Translate-Lauf erzeugte). Beide ergänzt, Lauf manuell nachgeholt (workflow_dispatch)
- 2026-08-04 (abends): Newsletter-Anmeldeseite gebaut und LIVE. newsletter.html (nur DE+EN) + Registrierung in translate-site.py, Footer-Links in index/hilfe (alle Sprachen, Cache-Trick → nur newsletter.html wurde übersetzt, 1 Sprache, Cent-Betrag; Workflow-Log bestätigt Cache-Hits für alle anderen Dateien). MailerLite-Formular im Dashboard angelegt (Gruppe `website`, Double-Opt-in AN, kein reCAPTCHA — eigenes Formular postet direkt an den Endpunkt). Live-Test: Anmeldung über linguaflow.app/newsletter.html → Abonnent `unconfirmed` in Gruppe `website` (Quelle „webform"), Testeintrag danach per `forget` entfernt. Nebenbei: Absender-Domain in MailerLite ist jetzt „Authenticated" (offener Punkt aus Juli erledigt), was-ist-neu.html in stabilen Enrichment-Zustand überführt (verhindert unnötige Mini-Neuübersetzung beim nächsten Lauf). Außerdem Apple-Relay-Registrierung erledigt: developer.apple.com → Services → „Sign in with Apple for Email Communication" → Domain linguaflow.app registriert, Status SPF grün → DOI-Mails erreichen jetzt auch „Hide My Email"-Adressen. Nachträglicher CSS-Fix: `.nl-form[hidden]{display:none}` (Formular blieb nach Erfolgsmeldung sichtbar)
- 2026-08-04 (nachmittags): „Was ist neu"-Seite committet (`f651d91`) + gepusht. Workflow lief sauber: NUR was-ist-neu.html übersetzt (1 Sprache EN, Cent-Betrag), keine anderen Dateien angefasst, sitemap.xml jetzt 246 URLs, Bot-Commit `944dd32` gepullt. Live-Check: linguaflow.app/was-ist-neu.html + /en/was-ist-neu.html → beide 200 OK
- 2026-08-04: „Was ist neu"-Seite gebaut (`was-ist-neu.html`, Design wie hilfe.html, eigene Timeline-Styles inline). In translate-site.py registriert: `SOURCE_FILES` + neues Set `DE_EN_ONLY_FILES` (= LEGAL_FILES + was-ist-neu.html) → nur EN-Übersetzung, `SCRIPT_VERSION` unverändert (kein Cache-Reset). Versionshistorie rekonstruiert aus iOS-Repo („LinguaFlow Apps beide/LinguaFlow iOS/…": git log, release-notes-v2.3/2.3.1, release-status.md). Offen: Commit/Push (Christian fragen), Navbar-/Footer-Verlinkung auf anderen Seiten, App-Integration (eigene Session)
- 2026-08-04: Doppelten Website-Ordner „website Linguaflow" aufgelöst. Befund: exakter Schnappschuss vom 24.06.2026, alle Website-Dateien byte-identisch mit Commit `85843d4` (bereits in der Git-Historie) — KEINE Übersetzungen betroffen, keine DeepL-Kosten. Einzigartige Planungs-Dokus gerettet: offene Features nach `.claude/features/` (dmarc-erweitern, marketing-backlog), Historisches nach `.claude/done/archiv-alter-ordner/` (altes TODO/DECISIONS/CLAUDE, FAQ-Recherche, SEO-Prompt). Offene Aufgaben (DMARC, Marketing) oben wieder eingetragen. Alter Ordner in den Papierkorb verschoben (darin auch 6 Simulator-Screenshots von April 2026, `img/Screenshots/`). `.DS_Store` in `.gitignore` ergänzt. Committet und gepusht (Commit `5453e97`).
- 2026-07-12: Rechtstexte nur noch DE+EN (LEGAL_FILES in translate-site.py, 96 Redirect-Stubs, Disclaimer-Satz in 3 Quelldateien) — einmalige Kosten ~1 € (3 Dateien × EN), Enrichment-Simulation vor Push bestätigte: keine ungewollte Neu-Übersetzung
- 2026-07-12: GSC-Fixes: index.html-Links → "./" (Quelle + alle Sprachordner, Cache-Hashes angepasst, keine Übersetzungskosten), 27 Redirect-Stubs für alte WordPress-404-URLs, 404.html neu
- 2026-07-11: 30-Tage-Testzeitraum (Jahresabo) auf Preiskarte + FAQ ergänzt (index.html, hilfe.html) → löst Neu-Übersetzung beider Dateien aus
- 2026-07-11: Ratgeber-Bereich Phase 1 komplett (Pipeline-Registrierung, Hub, 4 Artikel, Verlinkung, 404-Fix, Kosten-Bug-Fix)

## Wichtige Regeln für dieses Repo
- Deutsche Root-HTML-Dateien sind die Quelle; /<sprache>/-Ordner NIE von Hand bearbeiten (werden von scripts/translate-site.py überschrieben)
- Rechtstexte (datenschutz/eula/impressum, siehe LEGAL_FILES in translate-site.py) gibt es nur DE + EN; die übrigen /<sprache>/-Versionen sind Redirect-Stubs auf /en/ und werden vom Skript NICHT angefasst
- Jede `ratgeber*.html` im Root wird automatisch übersetzt und geht live — keine Entwürfe unter diesem Namen committen
- Jeder Edit an einer Quelldatei kostet die volle Neu-Übersetzung dieser Datei (~14–18 € bei Artikeln) → Änderungen bündeln, vor Commit Korrektur lesen
- `SCRIPT_VERSION` in translate-site.py NIE ohne Grund erhöhen (~70 € Voll-Neuübersetzung)
- Nach jedem Push: `git pull` vor dem Weiterarbeiten (Bot committet Übersetzungen)
- Markenwording: immer „inspiriert von der Birkenbihl-Methode"
